#include <stdio.h>
void main() {    
    // * && -> And, || -> Or, ! -> Not

    &&
    num = -3
    num -> odd && num -> prime && num >= 0 -> f  
        print num // 1 
    
    || 
    num = 2 
    num -> odd || num -> prime || num >= 0 -> t 


    ! 
    true 

}